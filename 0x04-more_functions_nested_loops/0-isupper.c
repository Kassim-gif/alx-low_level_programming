#include "main.h"

/**
 * _isupper - checks for tha uppercase character
 * @c: the character to be checked
 * Return: 1 if c is in tha uppercase, 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
