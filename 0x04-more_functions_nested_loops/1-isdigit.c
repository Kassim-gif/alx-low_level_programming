#include "main.h"

/**
 * _isdigit - checks for tha digit (0 through 9)
 * @c: int to be checked
 * Return: 1 if c is tha digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
